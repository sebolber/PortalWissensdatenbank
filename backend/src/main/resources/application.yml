server:
  port: 8080

spring:
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB

  datasource:
    url: ${DB_URL:jdbc:postgresql://portal-db:5432/portal}
    username: ${DB_USER:portal}
    password: ${DB_PASS:portal}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate:
        default_schema: public

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
    baseline-version: 0
    table: wb_schema_history
    validate-on-migrate: false

portal:
  jwt:
    secret: ${JWT_SECRET}
  core:
    base-url: ${PORTAL_CORE_BASE_URL:http://portal-backend:8080}
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:*}
  widget:
    registration-enabled: ${WIDGET_REGISTRATION_ENABLED:false}

logging:
  level:
    de.wissensdatenbank: INFO
